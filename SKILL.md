---
name: "prototype-annotator"
description: "为 HTML 原型生成批注工具，支持在 UI 元素上添加、查看和管理批注标记。当用户需要对原型页面添加批注、评论或审阅标记时调用。"
---

# 原型批注

## 一、悬浮控制栏与模式切换

在原型页面顶部居中悬浮四个按钮：
- **原型**：默认状态，可正常点击原型的所有交互
- **添加批注**：点击后切换为批注模式，原型所有交互禁用，页面停在当前状态；鼠标移动时**高亮预览**当前悬停的控件（加虚线框或半透明遮罩），让用户明确知道会选到哪个元素；可选择当前页面的任何控件（div），选择控件并单击后，小红点**记录相对于该控件的位置**（比例坐标）并停在该位置，同时自动弹出批注编辑弹框；添加后支持**拖拽小红点微调位置**；保存批注后**保持**在添加批注模式，可继续添加下一条
- **查看批注**：显示所有已添加的小红点；在查看批注模式下，原型交互**正常可用**（可点击按钮、关闭弹框等）；鼠标单击小圆点时，批注弹框紧贴圆点显示，同时自动定位并高亮目标元素；点击批注列表项时，目标元素在**原本位置**出现并高亮
- **保存到文件**：将当前所有批注数据以内嵌 `<script id="anno-data">` 标签形式写入 HTML 文件，实现批注与原型文件的持久化绑定；首次点击需选择原文件并授权写入权限，之后可一键保存

> **元素选择器唯一性**：批注绑定的目标元素选择器采用 `tag.className:nth-of-type(idx)` 格式逐级生成，确保在同级存在多个相同类名元素时仍能唯一匹配回原元素，避免圆点错位

> **动态面板/中继器适配**：当原型中存在动态面板（Tab切换、弹框等）时，各状态内的批注应在对应状态下显示，其他状态下隐藏；支持给当前不可见的动态面板状态添加批注

## 二、批注编辑弹框

- 弹框支持拖拽放大/缩小，最大可放大至屏幕80%；标题栏提供**最大化/还原**按钮一键切换
- 批注内容采用**富文本编辑器**（contenteditable），直接支持混排文字、表格、图片（粘贴截图/上传）、超链接，无需切换格式类型
- 富文本工具栏支持：加粗、斜体、下划线、**字体颜色**（10个预设色块：黑、蓝、绿、橙、红、紫、品红、青、金、灰，不可自定义取色）、插入表格、插入图片、插入链接
- 支持保存、编辑、删除
- 支持设置批注状态：待处理 / 已确认 / 已解决 / 驳回
- 查看批注时，弹框紧贴圆点显示，支持**放大/还原**

## 三、批注管理面板

- 侧边栏可展开/收起，显示当前页面所有批注列表，按页面分组
- 支持点击列表项定位到对应圆点位置并高亮
- 支持按状态、关键词搜索筛选
- 支持按状态筛选（如只看未解决）

## 四、数据持久化与共享

- 批注数据以JSON格式存储，结构包含：页面标识、目标元素选择器、相对位置比例、批注内容、状态、作者、时间戳
- 添加/编辑/删除批注时，数据自动保存到 localStorage（仅作为当前浏览器运行时缓存）
- **持久化到文件需主动点击"保存到文件"按钮**：通过 File System Access API（`showSaveFilePicker`）直接写回原 HTML 文件，批注数据以内嵌 `<script id="anno-data">` 标签形式注入到 `<head>` 中
- 首次保存需用户选择原文件并授权写入权限，之后点击"保存到文件"可一键写入；浏览器不支持 File System Access API 时回退为下载同名 HTML 文件，需手动替换原文件
- 保存时自动清理运行时元素（Echarts canvas、批注标记容器、弹框状态），避免下次加载时出现重复元素
- 将保存后的 HTML 文件发送给他人时，批注自动包含在内，换浏览器/换电脑均可查看
- 图片资源自动转为Base64内嵌，确保离线可用
- 若批注绑定的目标元素在原型迭代后被删除或结构变更，小红点显示为**失效状态**（灰色、带问号标识），批注内容仍可查看，弹框中提示"关联元素已变更，批注仅供参考"

## 五、快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl/Cmd + Shift + A` | 快速进入添加批注模式 |
| `Ctrl/Cmd + S` | 保存批注到文件（触发文件写入） |
| `Esc` | 退出批注模式或关闭弹框 |
| `Delete` | 删除当前选中的批注点 |
